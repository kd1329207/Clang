#include <stdio.h>
main()
{
	int no = 0, sum = 0, x = 0;
	for (no = 0; no != -999; no = no) {
		printf("®”‚ğ“ü—ÍF");
		scanf("%d", &no);
		sum += no;
		x++;
	}
	sum += 999;
	printf("‡Œv = %d\t•½‹Ï = %.2f", sum, (float)sum / x);
}